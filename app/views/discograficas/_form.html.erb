<%= bootstrap_form_for(@discografica) do |f| %>
  
	<%= f.alert_message "Datos incorrectos, corrija los siguientes datos:" %>
    <%= f.text_field :nombre %>
  	<%= f.text_area :descripcion %>
   	<%= f.submit %>
  
<% end %>
